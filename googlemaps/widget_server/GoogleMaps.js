// populate the 'data' object// e.g., data.table = $sp.getValue('table');(function () {    data.key = options.api_key || gs.getProperty('google.maps.key');    var gr = new GlideRecord("cmn_location");    gr.query();    var item,        locations = [];    while (gr.next()) {        item = {};        item.position = {};        item.position.lat = Number(gr.getValue('latitude'));        item.position.lng = Number(gr.getValue('longitude'));        item.title = gr.getDisplayValue();        locations.push(item);    }    data.locations = locations;})();